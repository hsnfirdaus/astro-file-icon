---
import type { IconType } from "./icon.type";

interface Props {
  type: IconType;
}
const { type } = Astro.props;
const { default: innerHTML } = await import(`./glyphs/${type}.svg?raw`);
---

<Fragment set:html={innerHTML} />
